// const context = require('./database'); // Connection Modülümüz.
const { format } = require('path');
const Ogrenci = require('./Ogrenci')

let x = new Ogrenci('celil','cavus',123);

// x.AddNote(50,60);
// x.GetById(3)
// x.UpdateNote(60,100,123);
// x.GetList();







